apiVersion: v1
kind: Service
metadata:
  name: coffee-shop-frontend
  labels:
    app: coffee-shop
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: coffee-shop
    component: frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP